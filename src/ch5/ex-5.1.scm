(define (factorial n)
  (define (iter product counter)
    (if (> counter n)
        product
        (iter (* counter product)
              (+ counter 1))))
  (iter 1 1))

; Data-Path:
; ==========

;   ┌───────┐   ┌───────┐    ,─.    ┌───────┐
;   │   p   │   │   c   ├──>( > )<──┤   n   │
;   └─────┬─┘   └───────┘    `─'    └───────┘
;     ^   │       │   ^
;     │   │       │   │           ^
;     X   │       │   X          /1\
;     │   │       │   │          ─┬─
;     │   │       ├───│───┐       │
;     │   v       v   │   v       v
;     │  ───────────  │  ───────────
;     │   \  mul  /   │   \  add  /
;     │    ───┬───    │    ───┬───
;     └───────┘       └───────┘


; Controller:
; ===========

;         start
;           │
;           │
;           v
;           ^
;          / \ yes
;   ┌────>( > )───> done
;   │      \ /
;   │       V
;   │       │
;   │       │no
;   │       v
;   │  ┌─────────┐
;   │  │  p<-mul │
;   │  └────┬────┘
;   │       │
;   │       v
;   │  ┌─────────┐
;   │  │  c<-add │
;   │  └────┬────┘
;   └───────┘
